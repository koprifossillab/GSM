# 판 이력

*"`v0.1.0` 이 뭐였지"* 를 한 곳에서 답하려고 둔다.

- **왜 그렇게 했는지**는 `devlog/` — 괄호 안의 번호가 그 문서다
- 날짜는 그 판을 붙인 날이다

## v0.3.0 — 2026-09-23 · 로고와 먹갈색, 그리고 캐시 (005)

**v0.2.x 의 손질이 화면에 하나도 안 떴다.** 코드가 아니라 캐시 탓이었다 —
nginx 가 CSS·JS 를 7 일간 `immutable` 로 내보내는데 주소가 판마다 같아서,
브라우저가 새 파일을 받지 않았다. 이 판부터 주소 끝에 내용 표(`?v=`)가 붙는다.

- **로고**를 붙였다 — 육각 틀 안의 나침반과 지질 해머. 패널 머리·설정 창·
  브라우저 탭 아이콘에 쓴다. 원본은 `docs/brand/`
- **대기 화면** — 첫 지도가 다 그려질 때까지 로고가 지어지는 움직그림과
  "불러오는 중" 을 띄운다. 덜 그려진 화면을 보이지 않는다
- **먹갈색 바탕에 금색 강조가 기본**이다. 로고의 색을 그대로 썼다. 앞 판은
  말만 그랬고 실제로는 크림색이었다. 밝은 바탕을 원하면 설정에서 **한지**
- **도구 손잡이를 지도 오른쪽 위**에 세로로 세웠다. 아이콘 아래에 이름을 적는다
- **페이지가 밀려 올라가던 것**을 막았다. 화면 읽개용 글자 하나가 패널
  밖으로 새어 페이지를 늘리고 있었다
- **기본 지질도 다섯 장**만 펼친다 — 100만·25만·5만·5만 도곽·지체구조도.
  나머지 56 종은 **추가 지질도** 안에 레이어군별로 접었다
- 팝업 첫 줄의 위경도를 **위도·경도 이름표를 달아** 크게 적는다
- **그리기** 박스 안을 두 칸으로 팠다 — "찍고 잰 것" 과 "점묶음"
- 축척 막대의 숫자와 눈금이 겹치던 것을 폈다

## v0.2.1 — 2026-09-23 · 손질

앞 판을 쓰면서 걸린 것들을 손봤다.

- **테마를 진한 갈색과 금색으로** 바꿨다. 대동여지도에서 이름을 딴 뷰어라
  옛 지도의 결을 쓴다. 금색은 아껴 쓴다 — 세는 표, 켜진 손잡이의 테,
  고른 칸의 밑줄뿐이다. 넓게 칠하면 값싸 보인다
- **설정 화면**을 제대로 지었다. 모양(밝게·어둡게·자동 / 고딕·명조·기기 기본 /
  글자 크기 셋) · 판 이력 · 지금 상태 세 칸이다. 고른 것은 이 브라우저에만 남는다
- 같은 이름 둘을 갈랐다 — 위쪽 탭은 **불러오기**(바깥 자료를 끌어오는 곳),
  그리기 칸의 폴더는 **점묶음**(끌어온 것이 지도에 그려진 결과)
- 단추를 한 벌로 맞췄다. 크기·모서리·아이콘 자리가 자리마다 달라 손대다 만
  것처럼 보였다
- "레이어 찾기" 칸을 치웠다. 레이어를 새로 들이지 않는 한 줄만 먹는다
- **패널이 안에서 구르게** 고쳤다. 바닥 CSS 가 앞선 손질에 함께 지워져
  화면 전체가 밀리고 있었다

## v0.2.0 — 2026-09-23 · 도구와 배경

배포한 뒤 화면을 보며 붙인 것들이다. 공식 뷰어가 주던 보조 기능을 옮겼다.

- **도구** — 점 찍기·거리 재기·넓이 재기. 어느 것도 저장하지 않는다
- **축척 막대**가 보인다. 넣어 두고도 좌표 막대가 덮고 있었다
- 팝업 **첫 줄이 언제나 누른 자리의 위경도**다. 속성이 무엇이 나오든
- 좌표를 찍어 이동하면 **5만 도폭 한 장**이 화면에 들어오게 맞추고, 그 자리를
  겹고리로 짚는다
- 레이어를 켤 때 투명도가 **50%**다. 배경을 깔고 보는 것이 예사라 처음부터
  밑이 비치게 둔다
- 배경 기본값이 **VWorld 위성**이고 **지명을 끄고 켤 수 있다** — 위성과 지명이
  따로 오는 레이어라 가능하다. 지질 경계를 볼 때 글자가 방해가 된다
- OpenStreetMap 을 고르개에서 뺐다. 이 망에서 막혀 있어 고를 수 있게 두면
  `403 Access blocked` 타일만 깔린다 (003)
- 왼쪽 패널을 **두 칸**으로 갈랐다 — "레이어 고르기" 와 "그리기".
  그리기 칸은 지금 지도에 있는 것을 폴더로 묶어 보인다
- 찍은 점을 **목록으로 저장**한다. 구글 지도의 "장소 저장" 과 같은 자리다 —
  임시 표시가 그대로 점묶음이 된다
- 톱니바퀴로 **설정과 판 이력**을 연다. 지금 무엇으로 돌고 있는지 함께 보인다
- 손잡이를 **지도 우측 아래로** 옮겼다. 아이콘만 남기고, 켜진 것은 색으로
  보인다. 다시 누르면 꺼지고 그때는 속성을 읽는다 — 그래서 "속성 읽기"
  단추가 없다. 잰 값은 손잡이 곁에 뜬다
- 지질도 네 장만 펼치고 나머지 57 종은 **"추가 주제도"** 로 접었다.
  줄이 길어 "그리기" 칸이 화면 밖으로 밀렸다
- 설정 단추를 패널 **바닥에 고정**했다. 스크롤해도 늘 보인다
- 박스가 박스로 보이게 했다 — 패널을 바탕색으로 낮추고 박스를 흰 카드로 띄운다

## v0.1.0 — 2026-09-23 · 뼈대

첫 판. 아직 인증키가 없어 상류에 실제로 타일을 받아보지 못했다.

- 상류 오픈API 를 훑고 문서의 오류를 확인했다 (001)
- Django 뼈대, 레이어 카탈로그 표, WMS·GetFeatureInfo 프록시
- 레이어 패널(켜기·순서·투명도·범례), 좌표 표시, 점묶음 업로드
- 처음 열면 5만 지질도가 켜진다 — 실제로 가장 많이 보는 축척이다
- 타일·범례를 디스크에 캐시한다. 같은 것을 두 번 받지 않는다
- 5만 지질도의 `도폭` 에 딸려 오는 원도 PDF·수치지질도 DOI 를 링크로 건다
- 옆 저장소(phyloserver)의 지질도 캐시는 **가져오지 않기로 했다** (002)
- 배경지도를 고르게 했다. 기본은 "없음" — OSM 이 기관 망에서 막혔다 (003).
  VWorld 열쇠를 넣으면 배경지도·위성+지명 둘이 고르개에 오른다
- Docker·nginx 서브패스 배포 뼈대. `http://paleolab/GSM/` 에 올렸다
