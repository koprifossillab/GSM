# 할 일

지금 무엇을 할 수 있는지 고르는 자리다. 왜 그렇게 했는지는 `devlog/`,
지금 어디까지 왔는지는 `HANDOFF.md`.

## 인증키가 나오면 — 이것부터

- [ ] `.env` 에 키를 채우고 `GSM_DEV_DIRECT_WMS=0` 으로 되돌린다
- [ ] `manage.py verify_layers` — 61 개가 `/openapi/wms` 로도 그려지는지 대조
- [ ] `GetLegendGraphic` 이 `/openapi/wms` 로도 되는지 본다. 안 되면 범례 단추를 내린다
- [ ] 결과를 devlog 002 에 적는다 — **몇 개가 실제로 열려 있었는지**가 요점이다
- [ ] 호출 제한에 걸리는 지점을 찾아둔다. 걸리면 `GSM_TILE_CACHE_SECONDS` 를 올린다

## VWorld 로 더 할 것 — 값 대비 쓸모 차례 (004)

조사는 끝났고 **쏴 보고 확인한 것들**이다. 자세한 것은 devlog 004.

### 값이 싸고 쓸모가 분명한 것

- [ ] **백지도 `white` 를 배경 고르개에 더한다** — 30 분.
      열쇠·경로·`z/y/x` 다 지금 것 그대로다. 지질도 밑에는 `Base` 보다 낫다
      (도로·지명 색이 죽어 있어 분홍·자홍과 안 다툰다).
      `midnight`(야간)도 되니 함께 넣을지 보고 정한다
- [ ] **점 하나로 묻는 것을 시료 지점에 붙인다** — 점을 찍거나 점묶음을
      올릴 때 한 번씩 부른다. 야장 정리에 곧바로 값을 한다
      - 좌표→지번·도로명 (`/req/address?request=getAddress`)
      - 읍면동 (`data=LT_C_ADEMD_INFO` + `POINT`)
      - 가장 가까운 단층 (`data=LT_L_GIMSFAULT` + `buffer=20000`)
      - 둘레 토박이 지명 (`data=LT_P_NSNMSSITENM` + `buffer=1000`)
      - **함정**: 데이터 API 의 자료 이름이 WMS 레이어명과 다르다
        (`LT_C_ADEMD` 는 안 되고 `LT_C_ADEMD_INFO` 라야 된다)

### 품이 좀 드는 것

- [ ] **VWorld WMS 를 레이어군 하나로 더 얹는다** — "지질 참고" 를 만들고
      단층·지질구조선·수문지질단위·등산로·국가지명·하천망·행정경계를 세운다.
      켜기·순서·투명도 얼개가 이미 있어 코드는 거의 안 는다
      - [ ] `kigam.py` 옆에 `vworld.py` 를 둔다 — 상류마다 문을 하나씩
            (CLAUDE.md "상류로 나가는 문은 하나")
      - [ ] **처음부터 서버 중계로 간다.** `GetFeatureInfo` 가 CORS 로 막혀
            어차피 중계가 필요하고, 중계하면 열쇠도 안 나가고 타일 캐시도 탄다
      - [ ] **한 장씩 쏴 보고 카탈로그를 만든다** (`data/vworld_layers.json`).
            `GetCapabilities` 에 있어도 빈 타일인 것이 있다 — 연속지적도·도로
            ·건물은 비었고 토양도는 자료 자체가 없다. 001 과 똑같은 함정이다
      - [ ] 자료 출처(국토지리정보원) 표기를 화면에 붙인다
- [ ] 단층을 **WFS 벡터로** 받아 선 색·굵기를 우리가 정한다. WMS 그림보다
      또렷하다. WFS 는 CORS 가 막혀 중계가 필수다

### 3D — 권하지 않는다. 하려면 먼저 재 본다

- [ ] (선행) 브라우저가 있는 데서 실험 페이지 하나로 **"VWorld 3D 지형 위에
      KIGAM WMS 가 걸쳐지나" 만 30 분 재 본다.** 이것이 안 되면 3D 는 접는다
- [ ] 된다면 `/GSM/3d/` 딴 화면으로 짓는다. OL 9.2.4 와 VWorld 가 심는
      OL 7.5.2 가 한 페이지에서 부딪히지 않게 갈라야 한다

**시료 고도(DEM)를 서버에서 일괄로 채우는 것은 못 한다.** 표고 타일 창구가
닫혀 있다 — 우리 키로도 엔진에 박힌 키로도 전부 `ERROR_DB_GENERAL` 이다.
3D 화면이 떠 있을 때 브라우저 안에서 뽑는 길만 남아 있다 (004).

## 다음 판 (v0.3)

- [ ] **주제도 비교** — 스와이프 슬라이더나 분할 화면. 처음에 고르신 넷 중
      하나인데 이번 판에 안 들어갔다. 레이어를 쌓는 것까지만 했다
- [ ] 레이어 순서를 끌어서 바꾸기. 지금은 `↑`·`↓` 단추뿐이다
- [ ] 켠 레이어와 보던 자리를 기억하기 (`localStorage`). 새로 고치면 다 풀린다.
      배경 고르개는 이미 기억한다 — 같은 자리에 붙이면 된다
- [ ] 배경지도를 켜면 지질도 투명도를 한 번 내려 주기. 지금은 100% 라
      배경을 덮어서, 사람이 슬라이더를 찾아 내려야 배경이 보인다
- [ ] 점묶음에 이름표 보이기 — 지금은 눌러야 뜬다
- [ ] 점묶음을 GeoJSON 으로 내려받기
- [ ] 레이어로 범위 맞추기 — `Layer.bbox` 를 넣어놓고 아직 쓰지 않는다
- [ ] **보고 있는 자리를 미리 데우기** — 지금 캐시는 WMS 변수를 sha256 으로
      묶어서 임의의 범위도 받지만, "이 도폭을 통째로 받아둬" 를 할 수 없다.
      phyloserver 처럼 z/x/y 격자로도 셀 수 있게 하면 된다 (devlog 002)

## 더 나중에

- [ ] 선·면 GeoJSON 받기. 지금은 Point 만 받고 나머지는 건너뛴다
- [ ] 좌표계 고르기. 지금은 화면이 EPSG:3857 에 고정이다.
      5186(중부원점)으로 찍힌 현장 자료를 받으려면 필요해진다
- [ ] 데이터셋 검색 API(`/openapi/data`) 붙이기 — 시료 자료를 지도에서 바로 찾기.
      이번 판은 WMS 만 쓴다
- [ ] 인쇄·내보내기 (지도 그림 한 장으로)
- [ ] Docker 이미지 실제로 굽고 `/srv/GSM` 에 올리기
- [ ] **5만 지질도 원도 스캔** — phyloserver 의 `uploads/geolmap/` 925 MB 에
      도폭별로 잘린 종이 지도 사진이 있다. WMS 로 받는 벡터와 겹치지 않는
      자료다. 도폭을 누르면 원도를 겹쳐 볼 수 있으면 좋겠다 (devlog 002)

## 하지 않기로 한 것

적어 두지 않으면 나중에 또 꺼내게 된다.

- **공간 연산 (GDAL·shapely·geopandas)** — 이 뷰어는 겹쳐 보고 클릭해 읽을 뿐이다.
  넣는 순간 web 이미지가 200 MB 대를 벗어난다. 필요해지면 그때 devlog 를 쓰고 더한다
- **DiaRUGA·ForGIA 와 DB 를 나누기** — 저장소도 배포도 따로다. 시추 지점을
  지도에 올리고 싶어지면 `Locality.lat/lon` 을 내보내 `PointSet` 으로 받는다
- **제품이 `/mgeo/geoserver` 를 타기** — 문서에 없는 주소다. 인증키가 들어오면
  끈다 (CLAUDE.md "두 개의 상류 주소")
- **네이버·카카오 지도를 배경으로 쓰기** — 둘 다 자기 렌더러를 들고 오는
  JS SDK 라, 쓰려면 OpenLayers 를 버리고 레이어 쌓기·투명도·속성 읽기
  ·점묶음을 그쪽 API 로 다시 짜야 한다. 배경지도 하나에 뷰어의 심장을 바꿀
  일이 아니다 (003)
- **OpenStreetMap 을 서버로 중계하기** — 화면은 살지만 그것이야말로 OSM 이
  막는 행동이다. 이번엔 서버 IP 가 막힌다 (003)
